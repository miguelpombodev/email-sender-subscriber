name: Build .NET Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Build Docker Image
        run: | 
          docker buildx build \ 
          --build-args INFISICAL_CLIENT_ID=${{ secrets.INFISICAL_CLIENT_ID }} \
          --build-args INFISICAL_CLIENT_SECRET=${{ secrets.INFISICAL_CLIENT_SECRET }} \
          --build-args INFISICAL_PROJECT_ID=${{ secrets.INFISICAL_PROJECT_ID }} \
          -t wallghost/email-sender-subscriber:latest \
          --load \
          .
      -
        name: Push Image to registry
        run: docker push wallghost/email-sender-subscriber:latest